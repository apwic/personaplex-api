# Cog configuration for Replicate deployment
# See: https://github.com/replicate/cog

build:
  python_version: "3.11"
  system_packages:
    - "ffmpeg"
  python_requirements: "requirements.replicate.txt"

# Predictive interface
predict: "predict.py:Predictor"

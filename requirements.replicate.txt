# Replicate-specific requirements with pinned versions to avoid timeout
# These versions are tested and work together

# Core dependencies
moshi==0.2.12
torch==2.5.1
torchaudio==2.5.1
huggingface-hub==0.26.5

# Audio processing (moshi dependencies)
numpy>=1.26.0,<2.3.0
safetensors>=0.4.0,<0.8.0
einops>=0.7,<0.9
sentencepiece>=0.2.0,<0.3.0
